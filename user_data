CREATE DATABASE user_data;

USE user_data;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nom VARCHAR(255),
    email VARCHAR(255),
    numero VARCHAR(20),
    date_naissance DATE,
    sexe ENUM('Féminin', 'Masculin'),
    wilaya VARCHAR(100),
    commentaire TEXT
);
